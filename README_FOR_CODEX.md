# CoreDesk Codex Handoff Pack

1. 把本目录内容复制到 CoreDesk 仓库根目录。
2. `docs/COREDESK_SPEC.md` 是唯一规范性实现文档。
3. 首次使用 Codex 时发送 `CODEX_MASTER_PROMPT.txt` 的内容，并指定 `M0`。
4. 后续每轮从 `MILESTONE_PROMPTS.md` 复制一个里程碑提示词。
5. 每轮结束检查 `IMPLEMENTATION_STATUS.md`，当前里程碑未通过测试时不要进入下一阶段。
6. Word 版规范用于人工审阅，不需要交给 Codex 解析。
